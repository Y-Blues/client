# client navigateur : plan d'implémentation (v2 — refonte « vrai framework »)

**Goal :** remplacer l'`ApiClient` HTTP bespoke de la v1 par quatre composants natifs (`RemoteCrud`, `RemoteDrafts`, `RemoteItemCatalog`, `RemoteServiceEndpoint`) implémentant réellement `ICrud`/`IDrafts`/`IItemCatalog`/`IServiceEndpoint`, auto-découverts par le vrai `ycappuccino.core.framework.Framework` dès que `bundle_prefix` contient `"ycappuccino.client"` — le code applicatif ne dépend que des interfaces `api`, jamais de `ycappuccino.client` directement. Documenter précisément, sans les diluer, les risques navigateur explicitement acceptés par l'utilisateur (thread OS réel pour `AsyncRunner`, PyYAML, `Framework.init` et son chemin de fichier, COOP/COEP).

**Architecture :** voir `client/docs/superpowers/specs/2026-09-15-client-design.md`. Le §0 (import-clean) reste le socle ; le §0bis (risques navigateur) et le §2 (décisions A.1–A.5) gouvernent tout le reste.

**Tech Stack :** inchangé (Python ≥ 3.10, `uv_build`, `unittest`), sauf que `ycappuccino-core` est maintenant une vraie dépendance d'exécution (le test d'intégration démarre un vrai `Framework`), pas seulement transitive.

## Contraintes globales

- Ne jamais modifier `api`, `core`, `storage`, `endpoints_storage`, `endpoints_service`, `http_server`, `permissions_app`, `scripts`, `scheduler`, `swagger`, `remote`, `component-creator`. Seul `hosts` reçoit un changement additif isolé (Partie C, commit séparé).
- Commit après chaque étape validée, dans `client` : `git -c user.name="Aurélien Pisu" -c user.email="aurelien.pisu@gmail.com" commit -m "client: <message court>"`. Un seul commit dans `hosts`, séparé : `hosts: <message court>`. Jamais de `git push`, jamais de réécriture d'historique.
- Toute affirmation sur le comportement en navigateur réel doit être marquée explicitement "non vérifié" — jamais prétendre l'avoir testé.
- Commande de test, depuis `client/` : `uv run python -m unittest discover -s src/unittest/python`. Depuis `hosts/` : la même.

## Task 1 : `HttpTransport` + `IHttpFetcher` + `decode_envelope`

**Files :**
- Create : `src/main/python/ycappuccino/client/transport.py`
- Create : `src/unittest/python/fake_fetcher.py`, `src/unittest/python/test_transport.py`
- Delete : `src/main/python/ycappuccino/client/http.py` (l'`ApiClient` v1), `src/unittest/python/test_http.py`, `src/unittest/python/fake_transport.py`

- [x] **Step 1** : `RawResponse` (repris tel quel de la v1), `IHttpFetcher(YCappuccinoComponent, ABC)` avec `async def fetch(method, url, headers, body) -> RawResponse`.
- [x] **Step 2** : `HttpTransport(YCappuccinoComponent)` : `__init__(configuration: Optional[IConfiguration] = None, fetcher: Optional[IHttpFetcher] = None)`, `start()` lit `client.base_url` sur `IConfiguration` (défaut `/api`), `set_token`/`get_token`/`clear_token`, `async def request(method, path, params=None, body=None) -> RawResponse` (encodage query JSON pour dict/list, `Content-Type`/`Authorization` conditionnels, délégation à `self._fetcher.fetch(...)` ou repli sur `pyodide_transport` importé paresseusement).
- [x] **Step 3** : `decode_envelope(response) -> dict`, adapté de `remote/call.py::_translate` (pas importé), levant `NotAuthenticated`/`Forbidden`/`NotFound`/`InvalidRequest`/`CrudError`.
- [x] **Step 4** : `fake_fetcher.py` (dict de routes -> `RawResponse`, même forme que l'ancien `FakeTransport`) + `test_transport.py` (base URL par défaut/configurée, query encoding, en-têtes, jeton, repli `RuntimeError` hors Pyodide, `decode_envelope` succès/chaque erreur).
- [x] **Step 5** : run -> `OK`.
- [x] **Step 6** : commit `git commit -m "client: HttpTransport + IHttpFetcher + decode_envelope, remplace ApiClient"`.

## Task 2 : `pyodide_transport.py` (adapté, pas réécrit)

**Files :** Modify `src/main/python/ycappuccino/client/pyodide_transport.py` (import `RawResponse` depuis `transport.py` au lieu de `http.py`, commentaires mis à jour : `Set-Cookie` illisible en `fetch`, cf. spec §0bis(d)).

- [x] Adapter, vérifier que `test_transport.py`'s cas de repli passe toujours.
- [x] commit (regroupé avec Task 1 dans l'historique réel : un seul commit couvre transport.py + pyodide_transport.py).

## Task 3 : `RemoteItemCatalog`

**Files :** Create `src/main/python/ycappuccino/client/remote_item_catalog.py`, `src/unittest/python/test_remote_item_catalog.py`.

- [x] Cache construit à `start()` (`GET /items`), `get_items`/`get_item`/`get_item_by_plural` servis depuis le cache (`NotFound` si absent), `get_schema`/`get_empty` résolvent le pluriel puis interrogent le réseau à chaque appel.
- [x] Tests : liste en cache, pas de refetch, `NotFound`, résolution schema/empty.
- [x] commit `git commit -m "client: RemoteItemCatalog, cache items au start()"`.

## Task 4 : `RemoteCrud` + résolution `item_id -> plural`

**Files :** Create `src/main/python/ycappuccino/client/remote_crud.py`, `src/unittest/python/fake_catalog.py`, `src/unittest/python/test_remote_crud.py`.

- [x] Les six méthodes d'`ICrud`, résolution du pluriel via `catalog: IItemCatalog` injecté (spec §3), `subject` ignoré silencieusement (spec §2 A.1) — un test dédié le vérifie explicitement (aucune fuite du `subject` dans les en-têtes/le corps).
- [x] Tests de succès (un par méthode, y compris `get_many`/`delete_many` avec `meta.size`/`deleted`) + tous les codes d'erreur (401/403/404/400/500).
- [x] commit `git commit -m "client: RemoteCrud, resolution item_id -> plural via IItemCatalog"`.

## Task 5 : `RemoteDrafts`

**Files :** Create `src/main/python/ycappuccino/client/remote_drafts.py`, `src/unittest/python/test_remote_drafts.py`.

- [x] Même schéma que `RemoteCrud`, cinq méthodes, routes `/drafts/<pluriel>/...`.
- [x] commit `git commit -m "client: RemoteDrafts"`.

## Task 6 : `RemoteServiceEndpoint`

**Files :** Create `src/main/python/ycappuccino/client/remote_service_endpoint.py`, `src/unittest/python/test_remote_service_endpoint.py`.

- [x] `call(name, method, extra_path, params, body, subject) -> ServiceResult`, route `/services/<name>[/<extra_path>...]`, un test dédié au flux de connexion (`login` -> jeton dans le corps -> `transport.set_token`).
- [x] commit `git commit -m "client: RemoteServiceEndpoint"`.

## Task 7 : test d'intégration réel (vrai `Framework`, vrai `bundle_prefix`)

**Files :** Create `src/unittest/python/test_client_framework.py`.

- [x] `TemporaryApplication` avec `bundle_prefix: [PACKAGE, "ycappuccino.client"]` ; `PACKAGE` définit un faux `IHttpFetcher` (`ycappuccino.client.transport.IHttpFetcher`) et un composant `Demo` dépendant de `ICrud`/`IItemCatalog`/`IServiceEndpoint` (jamais `ycappuccino.client` directement) — voir spec §2 A.4 pour le piège d'ordonnancement (le paquet du faux fetcher doit précéder `ycappuccino.client` dans `bundle_prefix`) et pourquoi il ne s'applique qu'à cette dépendance optionnelle-là.
- [x] Assertions : `Demo` reçoit bien un `ICrud`/`IItemCatalog`/`IServiceEndpoint` fonctionnels (résultats corrects), et tous les appels sont bien passés par le faux fetcher (aucun réseau réel).
- [x] commit `git commit -m "client: test d'integration framework reel (bundle_prefix inclut ycappuccino.client)"`.

## Task 8 : modèle partagé + composant de démo

**Files :** Modify `example/library/books.py` (inchangé), Create `example/library/catalog.py`; Modify `src/unittest/python/test_models.py` (remplace l'usage d'`ApiClient` par `RemoteCrud`).

- [x] `Catalog(YCappuccinoComponent)`, dépend de `crud: ICrud`/`catalog: IItemCatalog`, peuple `self.items` à `start()`.
- [x] `test_models.py` : round-trip document -> `Book` -> `get_storage_model()`, via `RemoteCrud` + faux fetcher/catalogue plutôt que l'ancien `ApiClient`.
- [x] commit `git commit -m "client: composant de demo Catalog (ICrud/IItemCatalog), test_models via RemoteCrud"`.

## Task 9 : README + spec + plan

**Files :** Rewrite `README.md`, `docs/superpowers/specs/2026-09-15-client-design.md`, ce fichier.

- [x] README : ce que le code applicatif écrit (jamais `ycappuccino.client` directement), session/authentification, résolution `item_id -> plural`, traduction d'erreurs, transport bas niveau, bootstrap navigateur détaillé, section « Risques d'exécution navigateur » (a)-(e), section « Limites et vérifications manuelles requises ».
- [x] commit `git commit -m "client: README + spec + plan, refonte v2"`.

## Task 10 : assets statiques navigateur (bootstrap complet)

**Files :** Rewrite `static/index.html`, `static/main.py`.

- [x] `index.html` : CDN pyscript épinglé (inchangé de la v1), avertissement explicite sur le besoin d'un `Host` `cross_origin_isolated=True` et d'un build Pyodide pthread.
- [x] `main.py` : séquence complète — `pyodide.loadPackage("pyyaml")` (via `pyodide_js`, non vérifié), `micropip.install(..., deps=False)` × 4 (`iPOPO`, `ycappuccino-api`, `ycappuccino-core`, `ycappuccino-client`), écriture de `conf/application.yml` + du paquet de démo dans le FS virtuel, `Framework().init(...)`, lecture du service `Catalog` résolu par DI, rendu DOM minimal.
- [x] Pas de test automatisé possible : relecture manuelle attentive à la place.
- [x] commit `git commit -m "client: bootstrap navigateur complet (vrai Framework), verification navigateur manuelle requise"`.

## Task 11 : `test_readme.py`

**Files :** Rewrite `src/unittest/python/test_readme.py`.

- [x] Rejoue les exemples CPython-exécutables du README (`Catalog` câblé à la main, `Login`/`set_token`, round-trip `Book`) ; exclut explicitement tout ce qui est spécifique au navigateur.
- [x] commit `git commit -m "client: test_readme.py, refonte v2"`.

## Task 12 : `pyproject.toml`

- [x] Vérifié : `ycappuccino-core` était déjà déclarée en dépendance `uv` normale (source éditable locale), pas seulement `-api` — rien à corriger ici, contrairement à ce que le mandat anticipait comme correction nécessaire.

## Task 13 : Partie C — `hosts` (commit séparé, dépôt séparé)

**Files (dans `hosts/`) :** Modify `src/main/python/ycappuccino/hosts/models/host.py` (`cross_origin_isolated: bool = False`), Modify `src/main/python/ycappuccino/hosts/servlet.py` (en-têtes COOP/COEP ajoutés à toute réponse d'un `Host` avec `cross_origin_isolated=True`, succès et erreurs), Modify `src/unittest/python/test_servlet_dispatch.py` (présent si `True`, absent par défaut, présent aussi sur une réponse d'erreur), Modify `README.md`.

- [x] Nouveau champ, défaut `False` (ne jamais casser un déploiement existant, ni un chargement CDN non préparé).
- [x] `HostServlet.handle()` restructuré : `_serve()` calcule la réponse, `handle()` ajoute les deux en-têtes après coup si le `Host` trouvé a `cross_origin_isolated=True` — uniforme sur tous les codes de statut.
- [x] README : section dédiée, avec la tension CDN documentée explicitement (§0bis(e) de la spec `client`).
- [x] `cd hosts && uv run python -m unittest discover -s src/unittest/python` -> `OK`, en comptant les tests avant/après.
- [x] commit, dans `hosts`, séparément : `git -c user.name="Aurélien Pisu" -c user.email="aurelien.pisu@gmail.com" commit -m "hosts: cross_origin_isolated opt-in COOP/COEP headers for pthread Pyodide clients"`.

## Task 14 : relecture finale

- [x] `cd client && uv run python -m unittest discover -s src/unittest/python` -> `OK`, compter les tests.
- [x] `cd hosts && uv run python -m unittest discover -s src/unittest/python` -> `OK`.
- [x] Relire la spec §0bis pour vérifier qu'aucune affirmation n'outrepasse ce qui a été réellement vérifié.
- [x] `git log --oneline` (les deux dépôts) pour vérifier l'historique.
